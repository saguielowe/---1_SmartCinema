# AI 协同开发记录（待提交前核对）

这不是固定模板。提交 `report.pdf` 前，请按实际使用情况删改并补充，保证能说明清楚“AI 提供了什么、小组自己决定了什么”。不要把未使用的工具或功能写进去。

## 本项目实际使用的场景

- 需求拆解和功能优先级讨论。
- 座位、订单和推荐结果的数据结构草案。
- 弧形座位图的 Canvas 实现思路。
- 无障碍模式、页面流程和文档提纲的讨论。
- 结合实际页面验收，迭代库存、账号摘要、座位提示和支付流程的布局与状态持久化。

## 小组的最终判断

- 推荐功能采用明确的规则和评分实现，不训练或部署大模型。
- 持久业务数据保存在 LocalStorage / 本地 JSON，不增加后端数据库；加分项使用本地 Python 标准库 WebSocket 服务同步临时选座，不负责持久化。
- 影厅按作业要求使用 Canvas 绘制；在固定的 10 排基础上，通过走道、空缺、偏移和弧度做布局变化。
- 模拟支付只用于演示订单和座位状态联动，不接真实支付。

## 提交报告时补充的内容

- 各成员实际负责的模块，以及哪些代码由成员完成或调整。
- 实现中遇到的具体问题，例如连续座位搜索、座位命中判断、LocalStorage 状态同步，并写出最终处理方式。
- 做过哪些可用性修改，例如减少页面跳转、把购票主线收成三步、提供大字体和高对比度。
- 使用过的模型/工具名称、日期和参考资料链接（仅列实际使用过的）。

## 成员记录

每个人认领模块后，在自己对应的小节补充实际情况。写一两句就够，重点是可核对，不要把没用过的内容补进去。

### A：选座组件

- 工具/用途：使用 OpenAI Codex 辅助检查 C 模块接口、实现 Canvas 交互选座/热度绘制、定位界面跳动和 LocalStorage 状态同步问题，并整理测试步骤与协作文档；协助排查团体票超过最长连座时无结果的问题，补充相邻座位区的兜底策略。
- 自己的修改或最终判断：人工确定座位状态与热度分层、默认隐藏热度、二次曲线弧形排布、游客演示流程、顶部账号摘要、无障碍状态按账号持久化，以及 B/D 的最终接口边界；每轮功能效果由成员在本地页面验收后继续调整。

### B：推荐与评分

- 工具/用途：使用 Codex 辅助整理推荐规则、实现连座搜索和评分解释、补充调试页与接口说明。
- 自己的修改或最终判断：推荐采用规则驱动而非模型驱动；评分按视角、距离、周边空位和偏好匹配加权，便于调试和报告说明。

### C：账号与订单

- 工具/用途：**C 负责人按实际情况填写。**
- 自己的修改或最终判断：**C 负责人按实际情况填写。**

### D：页面整合与无障碍

- 工具/用途：2026-07-23 使用 OpenAI Codex 核对 A/B/C 公共接口，辅助实现登录注册、订单筛选分页、响应式页面和无障碍模式，并通过本地浏览器验证游客购票、管理员权限、注册、移动端布局和显示设置。
- 自己的修改或最终判断：最终保留单页三步购票流程；无障碍拆为购票辅助与大字体、高对比度、色盲配色、减少动画、语音提示等独立开关；色盲状态同时用颜色和图形表达，避免只依赖红绿区别。
- 工具/用途：2026-07-26 使用 OpenAI Codex 辅助实现本地问答式意图解析、Ctrl/Cmd 区间拖选、Canvas 交互动画、WebSocket 多页面临时选座和技术约束审查，并生成自动化回归测试。
- 自己的修改或最终判断：热度正常模式按用户明确要求使用红/黄/绿；AI 顾问复用可解释规则而不伪装成云端大模型；多人功能仅同步临时选择，正式订单仍由 Store 管理；大字体提升到约 1.38 倍，高对比度使用黑白主色与黄色焦点轮廓。

## 可放入报告的简短说明

本项目在需求分析、数据结构讨论、Canvas 实现思路和文档整理阶段使用了 AI 辅助工具。小组根据作业要求和实现范围，对建议进行了筛选和修改：推荐部分采用可解释的规则实现，数据使用 LocalStorage 保存，最终页面交互、功能取舍和代码整合由小组成员完成。
