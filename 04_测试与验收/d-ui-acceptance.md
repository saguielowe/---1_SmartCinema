# D 模块测试与验收记录

测试日期：2026-07-26

## 测试环境

- 通过本地 HTTP 服务访问 `03_源码/index.html`
- 桌面视口：1440 × 900
- 手机视口：390 × 844
- 数据来源：项目内置 Mock 数据与浏览器 LocalStorage

## 验收结果

| 项目 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 游客主流程 | 选择 `s001`，确认推荐座位并模拟支付 | 锁座后弹出支付框；支付后显示“已支付”和取票码 | 通过 |
| 智能推荐接线 | 切换场次和购票条件 | 推荐座位、备选座位、体验评分和理由同步刷新 | 通过 |
| 普通用户登录 | 使用 `testuser / 123456` | 仅查看本人订单 | 通过 |
| 管理员登录 | 使用 `admin / admin123` | 显示管理员视图和全部用户订单 | 通过 |
| 新用户注册 | 输入合法用户名和两次一致密码 | 注册成功后自动登录，订单视图切换到新账号 | 通过 |
| 订单分页 | 管理员查看 4 笔订单 | 每页 3 笔，共 2 页，上一页/下一页状态正确 | 通过 |
| 订单筛选 | 筛选“已支付” | 只显示已支付订单，页码自动回到第 1 页 | 通过 |
| 无障碍购票辅助 | 开启总开关 | 自动切换后排偏好并勾选无障碍座位需求 | 通过 |
| 大字体/高对比度 | 开启对应设置 | 页面字号放大，文字、边框和焦点状态增强 | 通过 |
| 显示设置保存 | 开启大字体与高对比度，选择暖金幕后保存并刷新 | 弹窗立即关闭，刷新后两个开关和主题仍保留 | 通过 |
| 显示设置组合布局 | 分别检查普通、大字体、高对比度及两者同时开启；切换到 1440×900、840×1096、缩放等效 672×877 和 390×844 | 开关滑块居中且不贴边，设置卡片与操作区右缘对齐，无文字/主题重叠和页面横向溢出；手机仅设置列表滚动且操作按钮固定可见 | 通过 |
| 色盲友好 | 开启对应设置 | 图例和 Canvas 切换蓝/橙/紫配色，并增加点/线/叉标记 | 通过 |
| 减少动画 | 开启对应设置 | 页面过渡和推荐座位脉冲停止 | 通过 |
| 键盘操作 | 聚焦 Canvas，使用方向键和 Enter/空格 | 焦点移动并能选择座位，ARIA 提示同步更新 | 通过 |
| 手机响应式 | 切换到 390 × 844 | 主区域单列，无页面级横向溢出，Canvas 在卡片内滚动 | 通过 |
| 桌面响应式 | 切换到 1440 × 900 | 购票条件、座位图、订单信息保持三列 | 通过 |
| AI 问答顾问 | 输入“四口之家、儿童、过道、怕吵” | 自动解析 4 人并给出连续座位、视角、噪音、便捷性理由 | 通过 |
| Canvas 热度 | 开启热度图并切换日期 | Canvas 使用红/黄/绿展示热门/一般/冷门 | 通过 |
| 快速拖选 | Ctrl/Cmd 从一端快速拖到另一端 | 按起终点补齐完整座位区间，不因采样间隔漏座 | 通过 |
| 实时同步 | 两个页面选择同一场次 | 显示在线观众，第二个页面避让他人临时选择 | 通过 |
| 个性化主题 | 切换午夜青、星河紫、暖金幕 | 主色和背景氛围同步切换 | 通过 |
| iPad 响应式 | 切换到 1024 × 768 | 双列布局，无页面级横向溢出 | 通过 |

## 静态检查

- `app.js`、`advisor.js`、`realtime.js`、`seat-map.js`、`recommendation.js`、`store.js` 均通过 `node --check`。
- `advisor.test.mjs`、`accessibility-settings.test.mjs`、`requirements-static.test.mjs`、`seat-map-drag.test.mjs`、`realtime-websocket.test.mjs` 均通过。
- `realtime-server.py` 通过 Python 语法编译检查。
- `git diff --check` 通过。
- 浏览器控制台未发现项目 JavaScript 错误或警告。

## 提交前仍需全组确认

- 用最终合并分支再次执行一遍主流程，避免其他分支合入时产生回归。
- 补齐报告中的姓名、学号、邮箱、参考资料和各成员实际 AI 使用记录。
- 按最终提交机器的浏览器环境复核 `启动预览.cmd`。
